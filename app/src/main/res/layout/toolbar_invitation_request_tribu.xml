<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:fresco="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar_invitation_request_tribu"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimary"
    app:popupTheme="@style/AppTheme.PopupOverlay"
    app:titleTextAppearance="@style/ToolbarTextAppearance.Title"
    tools:showIn="@layout/activity_invitation_request_tribu">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:background="@android:color/transparent">

        <ImageView
            android:layout_width="25dp"
            android:layout_height="25dp"
            android:src="@drawable/ic_arrow_back"
            android:layout_centerVertical="true"
            android:id="@+id/arrow_back"
            android:background="@drawable/ripple_effect_arrow_buttom"/>

        <TextView
            android:id="@+id/tv_name_tribu"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginEnd="16dp"
            android:layout_marginStart="8dp"
            android:text="Solicitações enviadas"
            android:textSize="20sp"
            android:fontFamily="@font/nunito"
            android:textColor="@color/colorIcons"
            android:layout_centerVertical="true"
            android:textStyle="bold"
            android:layout_toEndOf="@+id/arrow_back"/>

    </RelativeLayout>
</android.support.v7.widget.Toolbar>